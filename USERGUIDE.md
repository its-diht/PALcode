
# How to Use

Compile the project with 

Instruction set is described in [the README](README.md).

Write code and save as `<filename.pal>`. 
if you're trying to run your PAL code with the original PAL java source, run with `java -cp out Main <file.pal> [nStacks]` where `[nStacks]` means the number of stacks your hypothetical machine will be using.